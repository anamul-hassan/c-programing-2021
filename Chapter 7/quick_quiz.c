# include <stdio.h>

int main(){
    // int i = 30;
    // int *ptr = &i;
    //     // Addition a number to a pointer.
    // printf("The value of i is %d \n", i);
    // printf("The address of i is %u \n", &i);
    // printf("The address of i is %u \n", ptr);
    // // after increment
    // ptr++;
    // printf("The address after increment of i is %u \n", ptr);

        // Subtreaction a number to a pointer.
    // printf("The value of i is %d \n", i);
    // printf("The address of i is %u \n", &i);
    // printf("The address of i is %u \n", ptr);
    // // after decrement
    // ptr = ptr -1;
    // // ptr--;
    // printf("The address after decrement of i is %u \n", ptr);
    
    // float i = 30;
    // float *ptr = &i;
        // Addition a number to a pointer.
    // printf("The value of i is %d \n", i);
    // printf("The address of i is %u \n", &i);
    // printf("The address of i is %u \n", ptr);
    // // after increment
    // ptr = ptr + 1;
    // ptr = ptr + 1;
    // // ptr++;
    // printf("The address after increment of i is %u \n", ptr);
    
    // Addition a number to a pointer.
    // printf("The value of i is %d \n", i);
    // printf("The address of i is %u \n", &i);
    // printf("The address of i is %u \n", ptr);
    // // after decrement
    // // ptr = ptr - 1;
    // // ptr = ptr - 1;
    // ptr--;
    // printf("The address after decrement of i is %u \n", ptr);

    char i = 'B';
    char *ptr = &i;
        // Addition a number to a pointer.
    // printf("The value of i is %d \n", i);
    // printf("The address of i is %u \n", &i);
    // printf("The address of i is %u \n", ptr);
    // // after increment
    // ptr++;
    // printf("The address after increment of i is %u \n", ptr);

    // Addition a number to a pointer.
    printf("The value of i is %d \n", i);
    printf("The address of i is %u \n", &i);
    printf("The address of i is %u \n", ptr);
    // after increment
    ptr--;
    printf("The address after increment of i is %u \n", ptr);


    return 0;
}